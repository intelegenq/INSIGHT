# The competition preview intentionally runs with zero paid services.
NEXT_PUBLIC_INSIGHT_DATA_MODE=demo

# Add provider credentials only when live collectors are enabled.
# Never commit actual secrets to this repository.
